# Rust
target/

# Tauri generated schemas
apps/desktop/src-tauri/gen/

# Node
node_modules/
apps/desktop/dist/

# OS
.DS_Store

# Logs and local captures (commit curated traces to fixtures/ deliberately)
*.log

# Raw research material (forum threads, manual scans) is working-copy only —
# never committed. Only the extracted, attributed facts (definition TOMLs) and
# the provenance ledger (research/SOURCES.md) go into git. See research/README.md.
research/revlimiter/
research/**/raw/
