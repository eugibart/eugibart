{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core window access plus the ability to open community-citation URLs in the system browser. The only URLs ever passed to the opener come from the compile-time-embedded guidance TOML (https-validated by the ecu-defs validator) — never from user input.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url"]
}
