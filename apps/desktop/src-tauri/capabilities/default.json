{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main-capability",
  "description": "Core window access plus two opener uses: (1) open community-citation URLs in the system browser — the only URLs ever passed come from the compile-time-embedded guidance TOML (https-validated by the ecu-defs validator), never from user input; (2) reveal app-produced files (CSV logs, health reports, wire traces) in the system file manager.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:allow-open-url", "opener:allow-reveal-item-in-dir"]
}
